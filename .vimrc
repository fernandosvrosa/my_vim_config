" Execucao do gerenciador de plugins 
execute pathogen#infect()

" Remapear atalho do Emmet
let g:user_emmet_leader_key=','

" Esquema de cores escuro 
colorscheme Tomorrow-Night-Eighties

" Tamanho da indentacao
set tabstop=2

" identifica o tipo de arquivo e indenta
filetype plugin indent on 

" colorir o editor
syntax on

" deixar coerente identacao com tamanho de TAB
set shiftwidth=2

" comportamento usuaril do backspace
set backspace=2

" Esse comando numera as linhas
set number

" fazer calculo da distancia das linhas 
set relativenumber

" busca incremental - feedback enquanto busco
set incsearch

" destaques nos resultador 
set hlsearch

" salvar a codiicacao desejada
set fileencoding=utf-8

" visualizar na codigicacao desejava
set encoding=utf-8

" usar espacos no logar de tab
set expandtab

" backspace respeitar indentacao
set softtabstop=2
